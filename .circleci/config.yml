version: 2.1

# Define the jobs we want to run for this project
jobs:
  build:
    docker:
       - image: cimg/base:stable
       #- image: circleci/node@12.9.1
    steps:
      - checkout
      #- run: npm install 
      - run: npm test